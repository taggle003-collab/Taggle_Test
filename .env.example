# Puppeteer Scraper Configuration
# Puppeteer browser automation for real website scraping
SCRAPER_HEADLESS=true
SCRAPER_DELAY=2000
SCRAPER_TIMEOUT=30000

# LLM API Configuration (OpenRouter or similar platform)
# API Key for accessing open-source models like DeepSeek-V3
LLM_API_KEY=

# Base URL for the LLM API (defaults to OpenRouter)
LLM_API_BASE_URL=https://openrouter.ai/api/v1

# Model to use for lead generation (recommended: deepseek/deepseek-chat for DeepSeek-V3)
# Alternative options: meta-llama/llama-3.3-70b-instruct, deepseek/deepseek-r1-distill-llama-70b
LLM_MODEL_NAME=deepseek/deepseek-chat

# If true, allow generating mock fallback leads / using mock fallback scrapers when OpenRouter fails.
# Recommended: leave unset/false in production to avoid masking configuration issues.
ALLOW_MOCK_FALLBACK=false

# If true, allow fallback to LLM and other scrapers when Puppeteer fails.
# Recommended: true in development, false in production to prioritize real scraped data.
ALLOW_FALLBACK_SCRAPERS=true

# Legacy Gemini API key (Google Generative AI) - kept for backward compatibility
GOOGLE_GENERATIVE_AI_KEY=
GEMINI_API_KEY=
